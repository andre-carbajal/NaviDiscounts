spring:
  datasource:
    url: jdbc:h2:mem:navi_discounts_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  bot:
    token: test-token
    scheduler:
      enabledExeCommand: false
      timeZone: UTC
      executionTime: "00:00"
